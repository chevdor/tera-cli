# ENV Demo with env-key

If you called `tera` using `--env` and `--env-key` set to `env` for instance, all the ENV varibles are located under `env`.

{% if LOGNAME %}
Hello **{{ env.LOGNAME }}**.
{% endif %}

This demo shows how your ENV variables can be used in a template.

- Home: {{ env.HOME }}
- Editor: {{ env.EDITOR }}
